name: First yaml ever

on:
  push:
    branches:
      - main

jobs:
  first_job_ever:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo 'Hello world!'
      - name: now it is
        run: date
      - name: Directory content
        run: ls